# Source photos and documents — the site inlines its own assets, so these
# are working files only.
*.jpg
*.jpeg
*.png
*.svg
*.zip
*.docx

# Raw Claude Design exports. index.html is the deployed export plus the fixes
# in .claude/apply-export.mjs, so each version is already captured in git
# history; these are multi-MB base64 blobs that git cannot delta-compress.
East Van SLP -  HTML Source/

# macOS
.DS_Store

# ...except the Open Graph card and the Apple touch icon, which Pages must serve as real files.
!og-image.jpg
!apple-touch-icon.png

# Google Search Console verification as downloaded. The copy that actually gets
# served lives at the repo root and IS tracked — Google re-checks it, so removing
# it would un-verify the property.
Search Console Verification/

# Stray canvas exports dropped in the repo root; the working copies belong in the
# source folder above.
Meaghan McLeod*.html
